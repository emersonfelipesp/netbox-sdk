# Cable Trace

## `nbx demo dcim interfaces get --id 1 --trace`

=== ":material-console: Command"

    ```bash
    nbx demo dcim interfaces get --id 1 --trace
    ```

=== ":material-text-box-outline: Output"

    ```bash
    nbx demo dcim interfaces get --id 1 --trace
    ```

    ```text
    (empty)
    ```

<span class="nbx-badge nbx-badge--err">exit&nbsp;124</span> <span class="nbx-badge nbx-badge--neutral">60.080s</span>

---

## `nbx demo dcim interfaces get --id 1 --trace-only`

=== ":material-console: Command"

    ```bash
    nbx demo dcim interfaces get --id 1 --trace-only
    ```

=== ":material-text-box-outline: Output"

    ```bash
    nbx demo dcim interfaces get --id 1 --trace-only
    ```

    ```text
    (empty)
    ```

<span class="nbx-badge nbx-badge--err">exit&nbsp;124</span> <span class="nbx-badge nbx-badge--neutral">60.045s</span>

---

## `nbx demo circuits circuit-terminations get --id 15 --trace`

=== ":material-console: Command"

    ```bash
    nbx demo circuits circuit-terminations get --id 15 --trace
    ```

=== ":material-text-box-outline: Output"

    ```bash
    nbx demo circuits circuit-terminations get --id 15 --trace
    ```

    ```text
    (empty)
    ```

<span class="nbx-badge nbx-badge--err">exit&nbsp;124</span> <span class="nbx-badge nbx-badge--neutral">60.047s</span>

---

## `nbx demo circuits circuit-terminations get --id 15 --trace-only`

=== ":material-console: Command"

    ```bash
    nbx demo circuits circuit-terminations get --id 15 --trace-only
    ```

=== ":material-text-box-outline: Output"

    ```bash
    nbx demo circuits circuit-terminations get --id 15 --trace-only
    ```

    ```text
    (empty)
    ```

<span class="nbx-badge nbx-badge--err">exit&nbsp;124</span> <span class="nbx-badge nbx-badge--neutral">60.075s</span>

---
