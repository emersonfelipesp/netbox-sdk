!!! note "Documented release"
    This site documents the **netbox-sdk 0.0.13 source candidate**. This is the final 0.0.13 tree awaiting publication to the default PyPI index through a GitHub Release. The latest final release on the default PyPI index is **0.0.12**; use `==0.0.12` for a reproducible normal-index install until 0.0.13 is published.
