"""Agent-evaluation assets: the scenario corpus and the types describing it.

Data only. Nothing here drives a model — the corpus is deliberately separable
from the harness that consumes it, so scenarios can be added, reviewed and
validated without model access or model cost.
"""
